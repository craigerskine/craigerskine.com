---
layout: false
tags: ux
title: Xpandy via Twind
desc: An interesting horizontal accordion effect for columns with background images.
tech:
  - title: HTML
    ico: fa6-brands:html5
  - title: CSS
    ico: fa6-brands:css3-alt
  - title: Twind
    ico: mdi:tailwind
  - title: Vue
    ico: mdi:vuejs
ico: mdi:arrow-expand-horizontal
color: neutral
date: 0011-01-01
---
<!doctype html>
<html lang="en" class="bg-transparent antialiased">

  <head>
    <meta charset="utf-8" />
    <meta http-equiv="x-ua-compatible" content="ie=edge" />
    <title>Xpandy - Twind</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
    <style>[v-cloak] { display: none; }</style>
  </head>

  <body class="bg-neutral-900 text-neutral-200 !block" style="display: none;">
    <div id="app" class="min-h-screen flex-(& col)" v-cloak>

      <main class="w-full flex-(& auto)">
        <section class="w-full flex">
          <a v-for="i in imgs" @click.prevent href="#" class="group w-1/4 px-4 text-white flex items-center justify-center relative motion-safe:(transition-all duration-300) hover:(w-full)">
            <span class="flex absolute inset-0 motion-safe:(transition duration-500)">
              <span class="border-l-(1 white/25) border-r-(1 black/50) bg-zinc-900/70 bg-gradient-to-br from-white/20 via-transparent backdrop-blur-lg flex items-center justify-center absolute inset-0 transition duration-500 group-hover:opacity-0">Hover</span>
              <img :src="'https://placehold.net/'+ (i + 3) +'.png'" :alt="'image' + i" class="w-full h-full object-(center cover)" />
            </span>
          </a>
        </section>
      </main>

    </div>
    <script type="module">
      // vue
      import { createApp } from 'https://esm.run/vue@3/dist/vue.esm-browser.js'
      createApp({
        data() {
          return {
            imgs: 4
          }
        },
      })
      .mount('#app')

      // twind
      import { install, injectGlobal } from 'https://esm.run/@twind/core';
      import presetAutoprefix from 'https://esm.run/@twind/preset-autoprefix';
      import presetTailwind from 'https://esm.run/@twind/preset-tailwind';
      install({
        presets: [presetAutoprefix(), presetTailwind()],
        darkMode: 'class',
        hash: false,
        theme: {
          extend: {
          },
        },
        rules: [
        ],
      });
      // global css
      injectGlobal`
        @layer base {
        }
      `
    </script>
  </body>

</html>

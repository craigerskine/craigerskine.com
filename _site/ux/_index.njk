---
layout: 'layouts/default.njk'
title: UX Experiments
permalink: /ux/
eleventyNavigation:
  key: Ux
  order: 2
---

{%- from 'components/hero.njk' import hero -%}

<section class="mx-auto py-6 px-4 max-w-7xl relative lg:px-8">
  {% call hero(title=title +' and other fun web/screen exercises',lead='Sometimes I need to build out web-based interfaces to measure the possibilities or to fully visualize design gaps and interaction issues. Here are a few that are not under embargo...') %}{% endcall %}
  <div class="py-16 sm:(py-32)">
    <ul class="grid-(& cols-1) gap-16 sm:(grid-cols-2) lg:(grid-cols-3)">
      {%- for item in collections.ux -%}
      <li class="pt-12 flex flex-col items-start relative">
        <div class="pt-6">
          <a href="{{ item.data.url if item.data.url else item.url }}" class="group text-gray-900 font-bold">
            {{ item.data.title }}
            <span class="h-12 w-12 text-2xl btn-{{ item.data.color }} absolute top-0 left-0" aria-hidden="true">
              <iconify-icon icon="{{ item.data.ico }}" inline class="iconify"></iconify-icon>
            </span>
            <span class="bg-{{ item.data.color }}-500/10 absolute -inset-3 z-20 opacity-0 rounded-2xl shadow-(xl {{ item.data.color}}-500/5) transform scale-95 motion-safe:(transition) group-hover:(opacity-100 scale-100)" aria-hidden="true"></span>
          </a>
          <p class="pt-2 text-(sm wrap-balance) leading-normal opacity-80">{{ item.data.desc }}</p>
          <div class="pt-4 text-xl flex-(& wrap) items-center justify-between">
            <ul class="flex gap-4 opacity-50">
              {%- for tech in item.data.tech %}
              <li><iconify-icon icon="{{ tech.ico }}" inline class="iconify"></iconify-icon></li>
              {%- endfor %}
            </ul>
            <p class="flex items-center gap-1">
              {%- if item.data.status %}<small class="text-xs uppercase opacity-50">{{ item.data.status }}</small>{%- endif %}
              <iconify-icon icon="mdi:link-variant" class="iconify opacity-30"></iconify-icon>
            </p>
          </div>
        </div>
      </li>
      {%- endfor -%}
    </ul>
  </div>
</section>

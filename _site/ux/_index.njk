---
layout: 'layouts/default.njk'
title: UX Experiments
permalink: /ux/
eleventyNavigation:
  key: Ux
  order: 2
---

{%- import 'components/hero.njk' as hero -%}

<section class="mx-auto py-6 px-4 max-w-7xl relative lg:px-8">
  {% call hero.x(title=title +' and other fun web/screen exercises',lead='Sometimes I need to build out web-based interfaces to measure the possibilities or to fully visualize design gaps and interaction issues. Here are a few that are not under embargo...') %}
  {% endcall %}
  <div class="py-16 sm:(py-32)">
    <ul class="grid-(& cols-1) gap-16 sm:(grid-cols-2) lg:(grid-cols-3)">
      {%- for item in collections.ux -%}
      <li class="relative flex flex-col items-start">
        <a href="{{ item.url }}" class="group">
          <div class="border-(t white/75) h-12 w-12 bg-{{ item.data.color }}-500/30 bg-gradient-to-b from-white/30 via-transparent to-{{ item.data.color }}-500/30 text-({{ item.data.color }}-600 2xl) flex items-center justify-center rounded-full shadow-(lg {{ item.data.color}}-500/30) ring-(1 inset {{ item.data.color }}-900/20) transform motion-safe:(transition) group-hover:(translate-y-1)" aria-hidden="true">
            <iconify-icon icon="{{ item.data.ico }}" width="1em" height="1em" inline="false" class="iconify"></iconify-icon>
          </div>
          <div class="pt-6">
            <strong class="text-gray-900">{{ item.data.title }}</strong>
            <p class="pt-2 text-sm leading-normal opacity-80">{{ item.data.desc }}</p>
            <div class="pt-4 text-xl flex-(& wrap) items-center justify-between">
              <ul class="flex gap-3 opacity-50">
                {%- for tech in item.data.tech %}
                <li><iconify-icon icon="{{ tech.ico }}" width="1em" height="1em" inline="false" class="iconify"></iconify-icon></li>
                {%- endfor %}
              </ul>
              <iconify-icon icon="mdi:link-variant" width="1em" height="1em" inline="false" class="iconify opacity-30"></iconify-icon>
            </div>
          </div>
          <div class="border-(t {{ item.data.color }}-500/20) bg-{{ item.data.color }}-500/10 absolute -inset-3 z-10 opacity-0 rounded-2xl transform scale-75 motion-safe:(transition) group-hover:(opacity-100 scale-100)" aria-hidden="true" aria-hidden="true"></div>
        </a>
      </li>
      {%- endfor -%}
    </ul>
  </div>
</section>